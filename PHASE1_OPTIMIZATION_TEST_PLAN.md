# Phase 1 优化测试计划

## 测试目标
验证 Phase 1 优化（文件缓存、上下文管理、Prompt Caching）是否正常工作并带来性能提升。

## 测试环境
- 分支: develop
- 构建状态: ✅ 成功编译
- 优化内容:
  1. 文件内容缓存层 (FileContentCache)
  2. 上下文窗口管理优化 (95% 阈值)
  3. AI SDK Prompt Caching (Anthropic ephemeral cache)

## 测试场景

### 场景 1: 文件缓存验证
**目的**: 验证文件缓存是否减少重复读取

**步骤**:
1. 创建一个需要多次读取同一文件的任务
2. 观察日志中的文件读取操作
3. 检查是否有缓存命中的迹象

**预期结果**:
- 同一文件在未修改的情况下，第二次读取应该从缓存获取
- 文件修改后，缓存应该自动失效

**验证方法**:
```bash
# 运行应用并创建测试任务
npm run dev

# 观察控制台日志，查找文件读取相关的日志
```

### 场景 2: 上下文窗口利用率
**目的**: 验证上下文窗口管理优化是否提高了利用率

**步骤**:
1. 创建一个包含大量上下文的任务
2. 观察上下文压缩的触发时机
3. 检查是否在 95% 阈值时才触发压缩

**预期结果**:
- 上下文压缩应该在接近 95% 时触发（而不是之前的 90%）
- 更多的对话历史可以保留在上下文中

**验证方法**:
```bash
# 查看 session/runner.ts 中的日志
# 关注 "Context window usage" 相关的日志
```

### 场景 3: Prompt Caching 效果
**目的**: 验证 Anthropic Prompt Caching 是否启用并生效

**步骤**:
1. 使用 Anthropic 提供商创建任务
2. 执行多个子任务
3. 观察 API 响应中的缓存相关指标

**预期结果**:
- API 响应应该包含 `cache_read_tokens` 和 `cache_creation_tokens`
- 后续请求应该显示缓存命中，减少输入 token 成本

**验证方法**:
```bash
# 检查任务的 token 使用统计
# 查看是否有 cacheReadTokens 和 cacheCreationTokens 字段
```

### 场景 4: 端到端性能对比
**目的**: 对比优化前后的整体性能提升

**测试任务**: 创建一个中等复杂度的功能任务（3-5个子任务）

**对比指标**:
- 总执行时间
- Token 使用量
- 文件读取次数
- 上下文压缩次数

**基准数据** (优化前):
- 需要从之前的任务执行中获取

**预期提升**:
- 执行时间: 减少 30-50%
- Token 使用: 减少 20-40% (得益于 Prompt Caching)
- 文件读取: 减少 40-60% (得益于文件缓存)

## 测试步骤

### 1. 启动应用
```bash
cd E:\Work\Aperant
npm run dev
```

### 2. 创建测试任务
创建一个简单的功能任务，例如：
- 添加一个新的工具函数
- 修改现有组件
- 添加单元测试

### 3. 监控执行过程
观察以下内容：
- 控制台日志中的缓存相关信息
- 任务执行时间
- Token 使用统计

### 4. 收集数据
记录以下指标：
- 任务开始时间
- 任务结束时间
- 总 token 使用量
- 缓存命中次数（如果有日志）
- 文件读取次数

## 调试技巧

### 启用详细日志
如果需要更详细的日志，可以在代码中添加调试输出：

```typescript
// 在 file-cache.ts 中
console.log('[FileCache] Cache hit:', filePath);
console.log('[FileCache] Cache miss:', filePath);

// 在 runner.ts 中
console.log('[PromptCache] Cache metadata:', experimental_providerMetadata);
```

### 检查缓存状态
```typescript
// 在 worker.ts 中添加
console.log('[FileCache] Stats:', fileCache.getStats());
```

## 已知限制

1. **文件缓存**:
   - 仅在单个 worker 会话内有效
   - 跨会话不共享缓存
   - 基于 mtime，可能在某些文件系统上不准确

2. **Prompt Caching**:
   - 仅对 Anthropic 提供商有效
   - 缓存 TTL 为 5 分钟
   - 需要足够长的系统提示词才能生效

3. **上下文管理**:
   - 95% 阈值可能在某些极端情况下仍然触发截断
   - 压缩策略可能影响对话连贯性

## 成功标准

✅ **通过条件**:
1. 应用正常启动和运行
2. 任务可以成功创建和执行
3. 没有明显的性能退化
4. 文件缓存在日志中可见（如果启用调试日志）
5. Token 使用统计中包含缓存相关字段（对于 Anthropic）

⚠️ **需要调查**:
1. 任务执行失败
2. 性能明显下降
3. 缓存相关错误
4. 内存使用异常增长

## 下一步

如果 Phase 1 测试通过，可以考虑：
1. 实施 Phase 2 优化（工具调用并行化、子任务并行处理）
2. 收集更多性能数据，优化缓存策略
3. 添加性能监控和指标收集
