{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build:vscode",
      "type": "shell",
      "command": "npm run build:vscode",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build",
      "problemMatcher": "$tsc",
      "detail": "Build the Autocode VS Code extension."
    }
  ]
}
